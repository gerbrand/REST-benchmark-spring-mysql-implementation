REST service benchmark contest implementation project using MySQL and JPA.

Requirements:
- Install mysql.
- Create initial database and modify properties to point to your new database.
- Java 6 and Maven

Run with
mvn jetty:run

You could also package your own .war and deploy to somewhere.

This implementation is MySQL backed and loads an initial set of data from src/main/resources/data.json.

